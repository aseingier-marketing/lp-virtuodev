---
---
<!-- Section 4 — Comment ça fonctionne (layout sticky gauche + cartes droite) -->
<section class="s4-section" id="section-comment-ca-fonctionne">
  <div class="s4-outer">

    <!-- Colonne gauche sticky -->
    <div class="s4-left">
      <!-- Label méthode -->
      <div class="s4-label">
        <span class="s4-label-line"></span>
        <span class="s4-label-text">M&eacute;thode</span>
      </div>

      <h2 class="s4-title">On relance vos devis<br>&agrave; votre place</h2>

      <p class="s4-desc">
        Vous gardez <strong style="color:#000000;">votre logiciel de devis</strong>. Vous envoyez vos PDF comme d&rsquo;habitude. Virtuodev fait le reste.
      </p>

      <p class="s4-compat">
        Fonctionne avec&nbsp;: <strong>Axonaut, Sellsy, Extrabat</strong>&hellip; et 20+ autres.<br>
        Z&eacute;ro migration. Z&eacute;ro formation.
      </p>

      <!-- Indicateur de progression -->
      <div class="s4-progress" aria-hidden="true">
        <button class="s4-dot s4-dot--active" data-target="0" aria-label="Étape 1"></button>
        <button class="s4-dot" data-target="1" aria-label="Étape 2"></button>
        <button class="s4-dot" data-target="2" aria-label="Étape 3"></button>
      </div>

      <button class="s4-cta" onclick="document.getElementById('section-callback').scrollIntoView({behavior:'smooth'})">
        Appel de qualification &mdash; 10 min &rarr;
      </button>
    </div>

    <!-- Colonne droite : cartes scrollables -->
    <div class="s4-right">

      <div class="s4-card" data-card="0">
        <div class="s4-card-header">
          <span class="s4-num">01.</span>
          <h3 class="s4-card-title">Vous gardez votre logiciel de devis&nbsp;&mdash; et vous envoyez votre PDF comme d&rsquo;habitude</h3>
        </div>
        <ul class="s4-list">
          <li><span class="s4-dot-bullet"></span><span>Pas de changement dans votre fa&ccedil;on de travailler. <strong>Vous faites exactement comme avant.</strong></span></li>
          <li><span class="s4-dot-bullet"></span><span>Vous envoyez votre devis en PDF &agrave; votre client. C&rsquo;est tout ce qu&rsquo;on vous demande.</span></li>
          <li><span class="s4-dot-bullet"></span><span>Aucune double saisie, aucune migration, aucune formation.</span></li>
        </ul>
        <p class="s4-result"><strong>R&eacute;sultat&nbsp;:</strong> z&eacute;ro friction. Votre logiciel de devis reste le m&ecirc;me &mdash; on se greffe dessus, invisiblement.</p>
      </div>

      <div class="s4-card" data-card="1">
        <div class="s4-card-header">
          <span class="s4-num">02.</span>
          <h3 class="s4-card-title">On lance automatiquement les relances</h3>
        </div>
        <ul class="s4-list">
          <li><span class="s4-dot-bullet"></span><span>Personnalis&eacute;es selon le comportement du prospect (ouvertures, clics).</span></li>
          <li><span class="s4-dot-bullet"></span><span>Envoy&eacute;es sur le bon canal&nbsp;: <strong>Email</strong>, <strong>SMS</strong> ou <strong>WhatsApp</strong>.</span></li>
          <li><span class="s4-dot-bullet"></span><span>Cadenc&eacute;es intelligemment pour ne pas &ecirc;tre intrusif.</span></li>
        </ul>
        <div class="s4-chips">
          <span class="s4-chip">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 7l-10 7L2 7"/></svg>
            Email
          </span>
          <span class="s4-chip">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
            SMS
          </span>
          <span class="s4-chip">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg>
            WhatsApp
          </span>
        </div>
        <p class="s4-result"><strong>R&eacute;sultat&nbsp;:</strong> chaque prospect re&ccedil;oit la bonne relance, sans que vous n&rsquo;ayez &agrave; y penser.</p>
      </div>

      <div class="s4-card" data-card="2">
        <div class="s4-card-header">
          <span class="s4-num">03.</span>
          <h3 class="s4-card-title">Prospect chaud&nbsp;? Vous signez.</h3>
        </div>
        <ul class="s4-list">
          <li><span class="s4-dot-bullet"></span><span>Pas de r&eacute;ponse imm&eacute;diate&nbsp;? On maintient le contact discr&egrave;tement.</span></li>
          <li><span class="s4-dot-bullet"></span><span>Les relances s&rsquo;arr&ecirc;tent automatiquement sur <strong>signature</strong> ou <strong>refus d&eacute;finitif</strong>.</span></li>
          <li><span class="s4-dot-bullet"></span><span>Vous &ecirc;tes notifi&eacute; quand un prospect montre un signal d&rsquo;int&eacute;r&ecirc;t fort.</span></li>
        </ul>
        <p class="s4-result"><strong>R&eacute;sultat&nbsp;:</strong> plus aucun devis ne tombe dans l&rsquo;oubli. Vous convertissez plus, sans travailler plus.</p>
      </div>

    </div>
  </div>
</section>

<style>
  /* ── Section wrapper ── */
  .s4-section {
    background: #FFFFFF;
    border-top: 1px solid var(--border-light);
    padding: 100px 0 120px;
  }
  .s4-outer {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 40px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: start;
  }

  /* ── Left sticky column ── */
  .s4-left {
    position: sticky;
    top: 120px;
  }
  .s4-label {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 24px;
  }
  .s4-label-line {
    display: block;
    width: 32px;
    height: 2px;
    background: #1D5FBF;
    border-radius: 2px;
  }
  .s4-label-text {
    font-family: var(--font-body);
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #1D5FBF;
  }
  .s4-title {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(28px, 3vw, 40px);
    color: var(--text-light-primary);
    letter-spacing: -0.03em;
    line-height: 1.12;
    margin: 0 0 20px;
  }
  .s4-desc {
    font-family: var(--font-body);
    font-size: 16px;
    color: var(--text-light-secondary);
    line-height: 1.7;
    margin: 0 0 16px;
    max-width: 380px;
  }
  .s4-compat {
    font-family: var(--font-body);
    font-size: 13.5px;
    color: var(--text-light-secondary);
    line-height: 1.6;
    margin: 0 0 32px;
  }
  .s4-compat strong {
    color: var(--text-light-primary);
    font-weight: 600;
  }

  /* Progress dots */
  .s4-progress {
    display: flex;
    gap: 8px;
    margin-bottom: 36px;
  }
  .s4-dot {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: rgba(29,95,191,0.18);
    border: none;
    padding: 0;
    cursor: pointer;
    transition: background 0.3s ease, width 0.3s ease;
  }
  .s4-dot--active {
    background: #1D5FBF;
    width: 24px;
  }

  /* CTA */
  .s4-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 14px 26px;
    border-radius: 11px;
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 16px;
    cursor: pointer;
    border: none;
    background: #1D5FBF;
    color: #FFFFFF;
    transition: background 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0px 10px 25px rgba(29,95,191,0.35);
  }
  .s4-cta:hover {
    background: #174EA6;
    transform: translateY(-2px);
    box-shadow: 0px 14px 32px rgba(29,95,191,0.45);
  }
  .s4-cta-sub {
    font-size: 12.5px;
    font-weight: 600;
    color: var(--text-light-secondary);
    margin-top: 10px;
    letter-spacing: 0.01em;
  }

  /* ── Right scrollable cards ── */
  .s4-right {
    display: flex;
    flex-direction: column;
    gap: 24px;
    padding: 4px 0 80px;
  }
  .s4-card {
    background: #FFFFFF;
    border: 1px solid var(--border-light);
    border-radius: 16px;
    padding: 32px 28px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.04);
    /* Initial state: invisible, shifted down */
    opacity: 0.25;
    transform: translateY(20px);
    transition: opacity 0.55s cubic-bezier(0.22,1,0.36,1),
                transform 0.55s cubic-bezier(0.22,1,0.36,1),
                border-color 0.4s ease,
                box-shadow 0.4s ease;
  }
  .s4-card.is-visible {
    opacity: 1;
    transform: translateY(0);
    border-color: rgba(29,95,191,0.22);
    box-shadow: 0 8px 32px rgba(29,95,191,0.10);
  }
  .s4-card.is-past {
    opacity: 0.2;
    transform: translateY(-12px);
  }

  /* Card content */
  .s4-card-header {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    margin-bottom: 20px;
  }
  .s4-num {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 28px;
    color: #2563EB;
    letter-spacing: -0.03em;
    line-height: 1;
    flex-shrink: 0;
    margin-top: 3px;
  }
  .s4-card-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(17px, 1.8vw, 21px);
    color: var(--text-light-primary);
    letter-spacing: -0.02em;
    line-height: 1.25;
    margin: 0;
  }
  .s4-list {
    list-style: none;
    margin: 0 0 16px;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .s4-list li {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    font-family: var(--font-body);
    font-size: 14.5px;
    color: var(--text-light-secondary);
    line-height: 1.55;
  }
  .s4-list li strong {
    color: var(--text-light-primary);
    font-weight: 600;
  }
  .s4-dot-bullet {
    flex-shrink: 0;
    width: 6px;
    height: 6px;
    border-radius: 999px;
    background: #2563EB;
    margin-top: 6px;
    opacity: 0.55;
  }
  .s4-chips {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 16px;
  }
  .s4-chip {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 4px 10px;
    border-radius: 999px;
    background: rgba(29,95,191,0.08);
    border: 1px solid rgba(29,95,191,0.20);
    font-size: 12.5px;
    font-weight: 600;
    color: #1D5FBF;
    letter-spacing: 0.01em;
  }
  .s4-result {
    font-family: var(--font-body);
    font-size: 14px;
    color: var(--text-light-secondary);
    line-height: 1.6;
    margin: 0;
    padding-top: 16px;
    border-top: 1px solid var(--border-light);
  }
  .s4-result strong {
    color: var(--text-light-primary);
    font-weight: 700;
  }

  /* ── Mobile ── */
  @media (max-width: 768px) {
    .s4-section {
      padding: 72px 0 80px;
    }
    .s4-outer {
      grid-template-columns: 1fr;
      gap: 40px;
      padding: 0 20px;
    }
    .s4-left {
      position: static;
    }
    .s4-title {
      font-size: 28px;
    }
    .s4-desc {
      max-width: 100%;
    }
    .s4-progress {
      display: none;
    }
    .s4-right {
      padding-bottom: 0;
    }
    .s4-card {
      opacity: 1 !important;
      transform: none !important;
      border-color: rgba(29,95,191,0.18) !important;
    }
  }

  /* Also keep s5-list styles needed below */
  .s5-list {
    list-style: none;
    margin: 16px 0 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .s5-list li {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    font-family: var(--font-body);
    font-size: 14.5px;
    color: var(--text-light-secondary);
    line-height: 1.5;
  }
  .s5-list li strong {
    color: var(--text-light-primary);
    font-weight: 600;
  }
  .s5-list-dot {
    flex-shrink: 0;
    width: 6px;
    height: 6px;
    border-radius: 999px;
    background: #1D5FBF;
    margin-top: 6px;
    opacity: 0.6;
  }
  @media (max-width: 768px) {
    .s5-features-grid {
      margin-top: 36px !important;
    }
    .s5-summary {
      margin-top: 36px !important;
      padding-top: 32px !important;
    }
  }

  /* ── Section 5 — Notre Différence ── */
  .s5-section {
    background: #EEF4FF;
    border-top: 1px solid rgba(29,95,191,0.10);
    padding: 100px 0 120px;
  }
  .s5-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 40px;
  }

  /* En-tête 2 colonnes (même logique que QuizCalculator) */
  .s5-head-wrap {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
    margin-bottom: 64px;
  }
  .s5-heading {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(30px, 3.8vw, 50px);
    letter-spacing: -0.04em;
    line-height: 1.1;
    color: var(--text-light-primary);
    margin: 0;
  }
  .s5-heading-accent { color: #1D5FBF; }
  .s5-subhead {
    font-family: var(--font-body);
    font-size: clamp(15px, 1.4vw, 17px);
    color: var(--text-light-secondary);
    line-height: 1.7;
    margin: 0;
    padding-top: 6px;
  }
  .s5-subhead strong {
    color: var(--text-light-primary);
    font-weight: 600;
  }

  /* 3 cartes */
  .s5-layout {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    align-items: start;
  }
  .s5-feature-card {
    background: var(--bg-light-card);
    border: 1px solid var(--border-light);
    border-radius: 16px;
    padding: 28px 24px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.04);
  }
  .s5-feature-card--alert {
    border-color: rgba(249,115,22,0.28);
    box-shadow: 0 0 0 3px rgba(249,115,22,0.06), 0 8px 28px rgba(249,115,22,0.10);
  }
  .s5-feature-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(17px, 1.6vw, 19px);
    color: var(--text-light-primary);
    letter-spacing: -0.02em;
    line-height: 1.25;
    margin: 0 0 10px;
  }
  .s5-feature-desc {
    font-family: var(--font-body);
    font-size: 14.5px;
    color: var(--text-light-secondary);
    line-height: 1.65;
    margin: 0 0 22px;
  }

  /* Mocks */
  .s5-mock {
    background: #F0F5FF;
    border: 1px solid rgba(29,95,191,0.10);
    border-radius: 12px;
    padding: 16px;
  }
  .s5-mock--alert {
    background: #FFF7F0;
    border-color: rgba(249,115,22,0.14);
  }
  .s5-mock-row {
    display: flex;
    align-items: center;
    gap: 7px;
    font-family: var(--font-body);
    font-size: 12.5px;
    color: var(--text-light-secondary);
    margin-bottom: 10px;
  }
  .s5-mock-row svg { color: #1D5FBF; flex-shrink: 0; }
  .s5-mock-count {
    display: flex;
    align-items: baseline;
    gap: 7px;
    margin-bottom: 10px;
  }
  .s5-mock-num {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 32px;
    letter-spacing: -0.04em;
    color: var(--text-light-primary);
    line-height: 1;
  }
  .s5-mock-label {
    font-family: var(--font-body);
    font-size: 13px;
    color: var(--text-light-secondary);
  }
  .s5-mock-dots {
    display: flex;
    gap: 5px;
  }
  .s5-mock-dot {
    flex: 1;
    height: 7px;
    border-radius: 999px;
    background: rgba(0,0,0,0.09);
  }
  .s5-mock-dot--filled { background: #1D5FBF; }
  .s5-mock-dot--hot {
    background: linear-gradient(90deg, #FB923C 0%, #F97316 100%);
  }
  .s5-mock-dot--pulse { animation: s5dotpulse 2s ease-in-out infinite; }
  @keyframes s5dotpulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(249,115,22,0); }
    50% { box-shadow: 0 0 0 3px rgba(249,115,22,0.38); }
  }
  .s5-mock-row--signed { color: #16A34A; font-weight: 600; font-size: 14px; }
  .s5-mock-check {
    width: 20px;
    height: 20px;
    border-radius: 999px;
    background: #16A34A;
    color: #FFFFFF;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .s5-mock-detail {
    font-family: var(--font-body);
    font-size: 13.5px;
    font-weight: 600;
    color: var(--text-light-primary);
    margin-bottom: 3px;
  }
  .s5-mock-time {
    font-family: var(--font-body);
    font-size: 12px;
    color: var(--text-light-secondary);
  }
  .s5-mock-alert-head {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-bottom: 8px;
  }
  .s5-mock-alert-name {
    font-family: var(--font-body);
    font-size: 13.5px;
    font-weight: 600;
    color: var(--text-light-primary);
    margin-bottom: 10px;
  }
  .s5-mock-alert-hint {
    font-family: var(--font-body);
    font-size: 12px;
    color: #EA580C;
    font-weight: 500;
    margin-top: 8px;
  }

  /* Live dot */
  .s5-live-dot {
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: #22C55E;
    position: relative;
    flex-shrink: 0;
  }
  .s5-live-dot::after {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 999px;
    background: rgba(34,197,94,0.28);
    animation: s5live 1.8s ease-in-out infinite;
  }
  @keyframes s5live {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(2.2); opacity: 0; }
  }
  .s5-live-label {
    font-family: var(--font-body);
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.09em;
    color: #16A34A;
  }

  @media (max-width: 768px) {
    .s5-section { padding: 72px 0 80px; }
    .s5-inner { padding: 0 20px; }
    .s5-head-wrap { grid-template-columns: 1fr; gap: 16px; margin-bottom: 40px; }
    .s5-layout { grid-template-columns: 1fr; }
  }
</style>

<script>
  (function () {
    const cards = Array.from(document.querySelectorAll('.s4-card'));
    const dots = Array.from(document.querySelectorAll('.s4-progress .s4-dot'));
    if (!cards.length) return;

    // Skip on mobile (cards are always visible)
    if (window.matchMedia('(max-width: 768px)').matches) return;

    function setActive(index) {
      cards.forEach((card, i) => {
        card.classList.remove('is-visible', 'is-past');
        if (i === index) card.classList.add('is-visible');
        else if (i < index) card.classList.add('is-past');
      });
      dots.forEach((dot, i) => {
        dot.classList.toggle('s4-dot--active', i === index);
      });
    }

    // IntersectionObserver: activate card when it enters the middle of viewport
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const index = parseInt(entry.target.dataset.card, 10);
            setActive(index);
          }
        });
      },
      {
        threshold: 0.55,
        rootMargin: '-10% 0px -10% 0px',
      }
    );

    cards.forEach((card) => observer.observe(card));

    // Dot click: scroll to card
    dots.forEach((dot, i) => {
      dot.addEventListener('click', () => {
        cards[i].scrollIntoView({ behavior: 'smooth', block: 'center' });
      });
    });

    // Init: activate first card after a short delay
    setTimeout(() => {
      const rect = cards[0].getBoundingClientRect();
      if (rect.top < window.innerHeight) setActive(0);
    }, 200);
  })();
</script>

<!-- Section 5 — Notre Différence -->
<section class="s5-section" id="section-notre-difference">
  <div class="s5-inner">

    <!-- En-tête 2 colonnes -->
    <div class="gs-hidden s5-head-wrap" data-gs="s5" data-gs-delay="0">
      <h2 class="s5-heading">Vos devis ont le suivi<br>qu&rsquo;ils <span class="s5-heading-accent">m&eacute;ritent.</span></h2>
      <p class="s5-subhead">On sait quand un prospect <strong>ouvre son devis</strong>, <strong>combien de fois</strong>, et si les relances ont suffi &agrave; <strong>le faire signer</strong>. Vous n&rsquo;intervenez que si c&rsquo;est vraiment n&eacute;cessaire.</p>
    </div>

    <!-- 3 cartes -->
    <div class="s5-layout">

      <!-- Card 1 — Tracking -->
      <div class="gs-hidden s5-feature-card" data-gs="s5" data-gs-delay="0.1">
        <h3 class="s5-feature-title">Votre devis a des yeux</h3>
        <p class="s5-feature-desc">On vous notifie d&egrave;s la premi&egrave;re ouverture. Vous savez combien de fois votre prospect l&rsquo;a consult&eacute;, et la relance suivante s&rsquo;adapte en cons&eacute;quence.</p>
        <div class="s5-mock">
          <div class="s5-mock-row">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
            Lo&iuml;c B. a ouvert son devis
          </div>
          <div class="s5-mock-count">
            <span class="s5-mock-num">3</span>
            <span class="s5-mock-label">fois aujourd&rsquo;hui</span>
          </div>
          <div class="s5-mock-dots">
            <span class="s5-mock-dot s5-mock-dot--filled"></span>
            <span class="s5-mock-dot s5-mock-dot--filled"></span>
            <span class="s5-mock-dot s5-mock-dot--filled"></span>
            <span class="s5-mock-dot"></span>
            <span class="s5-mock-dot"></span>
            <span class="s5-mock-dot"></span>
          </div>
        </div>
      </div>

      <!-- Card 2 — Signature -->
      <div class="gs-hidden s5-feature-card" data-gs="s5" data-gs-delay="0.2">
        <h3 class="s5-feature-title">La relance m&egrave;ne &agrave; la signature</h3>
        <p class="s5-feature-desc">Le prospect clique sur le lien dans la relance et signe directement en ligne. Les relances s&rsquo;arr&ecirc;tent automatiquement &mdash; vous &ecirc;tes notifi&eacute;.</p>
        <div class="s5-mock">
          <div class="s5-mock-row s5-mock-row--signed">
            <span class="s5-mock-check">
              <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 6L9 17l-5-5"/></svg>
            </span>
            Devis sign&eacute;
          </div>
          <p class="s5-mock-detail">Samuel P. &mdash; 4&nbsp;200&nbsp;&euro;</p>
          <p class="s5-mock-time">Il y a 12 min</p>
        </div>
      </div>

      <!-- Card 3 — Alerte -->
      <div class="gs-hidden s5-feature-card s5-feature-card--alert" data-gs="s5" data-gs-delay="0.3">
        <h3 class="s5-feature-title">6 ouvertures sans r&eacute;ponse&nbsp;? On vous pr&eacute;vient.</h3>
        <p class="s5-feature-desc">Vous recevez une alerte avec le nom du prospect et le montant du devis. C&rsquo;est le moment de donner le coup de fil d&eacute;cisif.</p>
        <div class="s5-mock s5-mock--alert">
          <div class="s5-mock-alert-head">
            <div class="s5-live-dot"></div>
            <span class="s5-live-label">Prospect chaud</span>
          </div>
          <p class="s5-mock-alert-name">Thomas M. &mdash; 5&nbsp;800&nbsp;&euro;</p>
          <div class="s5-mock-dots">
            <span class="s5-mock-dot s5-mock-dot--hot"></span>
            <span class="s5-mock-dot s5-mock-dot--hot"></span>
            <span class="s5-mock-dot s5-mock-dot--hot"></span>
            <span class="s5-mock-dot s5-mock-dot--hot"></span>
            <span class="s5-mock-dot s5-mock-dot--hot"></span>
            <span class="s5-mock-dot s5-mock-dot--hot s5-mock-dot--pulse"></span>
          </div>
          <p class="s5-mock-alert-hint">A ouvert 6 fois &mdash; appelez maintenant</p>
        </div>
      </div>

    </div>

  </div>
</section>
